% Packages used only by the "LaTeX Hints" example sections.
%
% pgfplots: plot functions and data directly in LaTeX (loads tikz)
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
%
% longtable: tables spanning multiple pages
\usepackage{longtable}
