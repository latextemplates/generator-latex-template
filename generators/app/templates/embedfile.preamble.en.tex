% Embed the bibliography data into the PDF so that readers can extract it
% (e.g., with JabRef via "Import into database" on the PDF)
\usepackage{embedfile}
\embedfile[mimetype=application/x-bibtex,desc={BibTeX entries of all references}]{<%= filenames.bib %>.bib}
