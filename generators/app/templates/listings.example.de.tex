<%= heading2 %>{Quellcode}

<%- bexample %>
<% if (cleveref) { -%>
\Cref{lst:XML} zeigt XML-Quelltext.
<% } else { -%>
Listing~\ref{fig:label} zeigt XML-Quelltext.
<% } -%>

\begin{lstlisting}[
  language=XML,
  caption={Beispiel-XML-Listing},
  label={lst:XML}]
<listing name="first sample">
  <!-- comment -->
  <content>not interesting</content>
</listing>
\end{lstlisting}
<%- eexample %>

Der zusätzliche Paramter \verb+float+ führt dazu, dass das Listing auch floatet.

One can also typeset JSON as shown in <% if (cleveref) { %>\cref<% } else { %>Listing~<% } %>{lst:flJSON}.

<%- bexample %>
\begin{lstlisting}[
  float,
  language=json,
  caption={Example JSON Listing -- placed as floating figure},
  label={lst:flJSN}]
{
  key: "value"
}
\end{lstlisting}
<%- eexample %>
