<%= heading2 %>{Tables}
<% if (documentclass == 'ieee') { -%>

Note that IEEE does not support \verb+\begin{table}+, one has to use \verb+\begin{figure}+.
<% } -%>

<%- bexample %>
\begin{<% if (documentclass == 'ieee') { %>figure<% } else { %>table<% } %>}
  \caption{Simple Table}
  \label{tab:simple}
  \centering
  \begin{tabular}{ll}
    \toprule
    Heading1 & Heading2 \\
    \midrule
    One      & Two      \\
    Thee     & Four     \\
    \bottomrule
  \end{tabular}
\end{<% if (documentclass == 'ieee') { %>figure<% } else { %>table<% } %>}
<%- eexample %>

<%- include('diagbox.example.en.tex', this); %>
